---
import "@fontsource-variable/onest"
/* import Footer from "../components/Footer.astro" */
import Header from "../components/Header.astro"
import Footer from "../components/Footer.astro"
interface Props {
  title: string
  description: string
}
const { description, title } = Astro.props
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>

  <body>
    <div class="relative h-full w-full bg-slate-950">
      <!-- Primer círculo -->
      <div class="absolute bottom-0 left-[-10%] md:left-[-20%] top-[-10%] h-[300px] w-[300px] md:h-[800px] md:w-[800px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.5),rgba(255,255,255,0))]"></div>
      
      <!-- Segundo círculo -->
      <div class="absolute bottom-0 right-[-10%] md:right-[-20%] top-[-10%] h-[300px] w-[300px] md:h-[800px] md:w-[800px] rounded-full bg-[radial-gradient(circle_farthest-side,rgba(255,0,182,.5),rgba(255,255,255,0))]"></div>
    </div>
    <Header relative/>
    <slot />
    <Footer />
  </body>
 

</html>

<style is:global>
  :root {
    color-scheme: light dark;
  }

  html, body {
    overflow-x: hidden;
  }

  html {
    font-family: "Onest Variable", system-ui, sans-serif;
    scroll-behavior: smooth;
  }

  body {
    background: #0c0000;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }
  }

  /* Animación de escritura */
  .typewriter {
  overflow: hidden; /* Esconde el texto que aún no ha sido "escrito" */
  white-space: nowrap; /* Asegura que el texto no haga saltos de línea */
  animation: typing 4s steps(40, end), blink 0.75s step-end infinite;
  display: inline-block; /* Asegura que el ancho del elemento sea adecuado para el contenido */
}

@keyframes typing {
  from {
    width: 0;
  }
  to {
    width: 100%; /* Mantiene el texto completamente visible al terminar */
  }
}

@keyframes blink {
  from {
    border-right-color: rgba(255, 255, 255, 0.75);
  }
  to {
    border-right-color: transparent;
  }
}

</style>
